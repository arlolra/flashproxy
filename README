http://opensource.adobe.com/wiki/display/flexsdk/Flex+SDK

Install the Flex SDK and put its bin directory in your PATH.

To build, run

$ mxmlc swfcat.as

To serve a crossdomain.xml file, use a netcat server such as Ncat:

# ncat -lkv 843 --sh-exec "cat crossdomain.xml"
